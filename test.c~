#include <stdio.h>
#include <stdlib.h>

int main(){
	FILE *fichier;
	fichier = fopen("test.bin", "wb");
	int var = 15;
	fwrite(&var, sizeof(var), 1, fichier);
	printf("%d\n", fread(&var, sizeof(var), 1, fichier));
}
